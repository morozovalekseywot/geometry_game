#pragma once

#include "color.h"

const int bounds_size = 50;

const Color bounds_color = Color(242, 167, 53);
const Color circle_color = Color(255, 255, 255);
const Color projectile_color = Color(250, 0, 0);
const Color bonus_color = Color(0, 255, 100);
const Color freeze_color = Color(50, 250, 250);

const int cube_limit = 4;
const double freeze_time = 1.0;
const double wait_after_press = 0.2;
